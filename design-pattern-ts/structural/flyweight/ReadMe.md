flyweight pattern is used to share states across various objects.

Especially useful in scenarios where we same data or states that can be shared across different objects.

intrinstic -> statis data not changing.
extrinstic -> changing values.

Flyweight can maintain there own states and let other handle the extrintic state.

Or it can become composite flyweight -> handles both in and extrinstic states internally.